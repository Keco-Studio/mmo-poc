{
  "name": "mmo-poc",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "playtest": "playwright test",
    "preview": "vite preview",
    "generate:assets": "tsx scripts/generate-placeholder-assets.ts",
    "validate": "tsx scripts/validate.ts",
    "report": "tsx scripts/write-playtest-report.ts"
  },
  "dependencies": {
    "excalibur": "^0.30.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "pngjs": "^7.0.0",
    "tsx": "^4.22.3",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
